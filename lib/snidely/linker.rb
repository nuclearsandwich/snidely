class Snidely::Linker
	def self.link file_path, link_path
		link_tree = link_path.split '/'
		link_accum = ""
		while link_tree.length > 1
			Dir.mkdir "#{ENV['HOME']}link_accum << link_tree.shift
		end
	end

	def self.mkdir_with_parent 
end
